const d = new Date();
var month_ = d.getMonth();
export const initialState = {
	month: month_
}

const reducer = (state, action) => {
	switch(action.type){
		case "SET_AVAILABLE_SLOT":
			return {

			}
	}
}

export default reducer;
